apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: round-trip-test
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/bakerlunch-ai/midas.git
    path: deploy/round-trip-test
    targetRevision: main
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
