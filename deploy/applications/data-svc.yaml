apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: data-svc
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/bakerlunch-ai/midas.git
    path: deploy/data-svc
    targetRevision: main
  destination:
    server: https://kubernetes.default.svc
    namespace: data-svc
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
