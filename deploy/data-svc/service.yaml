apiVersion: v1
kind: Service
metadata:
  name: data-svc
  namespace: data-svc
spec:
  type: ClusterIP
  selector:
    app: data-svc
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
