apiVersion: v1
kind: Service
metadata:
  name: hello-svc
  namespace: hello-svc
spec:
  type: ClusterIP
  selector:
    app: hello-svc
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
